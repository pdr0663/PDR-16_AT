# Arduino IDE 1.8.x

Version-specific support files for the legacy Arduino IDE 1.8.x toolchain.

Use this subtree for the older `arduino-builder`-based setup and any
machine-specific paths for the legacy install.

Current resolver:

- `resolve_arduino_root.cmd`
